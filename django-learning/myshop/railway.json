{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile"
    },
    "deploy": {
        "startCommand": "gunicorn myshop.wsgi:application --bind 0.0.0.0:$PORT",
        "healthcheckPath": "/api/products/",
        "restartPolicyType": "ON_FAILURE"
    }
}